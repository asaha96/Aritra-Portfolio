declare module "motion/react" {
  // Minimal typing to keep TS happy until dependencies are installed.
  // After `npm install`, the real types (if provided by the package) will take over.
  export const motion: any;
}


